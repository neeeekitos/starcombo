import {DexCombo} from "../constants/interfaces";
import {Singleton} from "./singleton";

export class JediSwap extends Singleton implements DexCombo {

  addLiquidity(): void {
  }

  approve(): void {
  }

  mint(): void {
  }

  removeLiquidity(): void {
  }

  revoke(): void {
  }

  swap(): void {
  }

}
